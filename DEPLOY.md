# 🌍 איך מעלים את האתר לאינטרנט (בחינם)

בסוף התהליך תהיה לך **כתובת אינטרנט אמיתית** כמו
`https://kaytanot-rg.onrender.com` – כל ילד בבית הספר ייכנס אליה מהטלפון,
יראה את אותם דירוגים ואת אותן הודעות.

הכול חינם ובלי כרטיס אשראי. לוקח בערך 15 דקות. עדיף לעשות את זה עם מבוגר.
צריך שני חשבונות חינמיים: **GitHub** (המקום שבו הקוד שוכב) ו-**Render** (המקום שמריץ את האתר).

> אם רוצים שהדירוגים יישמרו לאורך זמן ולא יתאפסו – מוסיפים גם מסד נתונים.
> ההסבר על זה נמצא בסוף המדריך, ואפשר להוסיף אותו גם אחר כך.

---

## שלב 1 · חשבון GitHub (המקום שבו הקוד שוכב)

1. נכנסים ל-https://github.com ולוחצים **Sign up**.
2. נרשמים עם אימייל וסיסמה (צריך לאשר את האימייל).
3. אחרי ההרשמה לוחצים על **+** בפינה למעלה → **New repository**.
4. ב-**Repository name** כותבים: `kaytanot-rg`
5. בוחרים **Public** ולוחצים **Create repository**.
6. במסך שנפתח לוחצים על הקישור **uploading an existing file**.
7. **גוררים לשם את כל הקבצים מהתיקייה** – server.js, camps.json, package.json,
   render.yaml, README.md, וגם את **התיקייה public** (גוררים אותה כמו שהיא).
   ⚠️ לא לגרור את `data.json` ולא את `node_modules` אם קיימים.
8. למטה לוחצים **Commit changes**.

---

## שלב 2 · Render (המקום שמריץ את האתר)

1. נכנסים ל-https://render.com ולוחצים **Get Started** → מתחברים עם **GitHub**.
2. לוחצים **New +** → **Web Service**.
3. בוחרים את המאגר `kaytanot-rg` שיצרת בשלב 1 → **Connect**.
4. ממלאים:
   - **Name**: `kaytanot-rg` (זה יהיה חלק מהכתובת)
   - **Runtime / Language**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Instance Type**: **Free**
5. אפשר לדלג על שאר ההגדרות. (אם רוצים לשנות את קוד המנהל – גוללים ל-
   **Environment Variables**, מוסיפים משתנה בשם `ADMIN_CODE` ובוחרים קוד.
   בלי זה הקוד נשאר `1108`.)

6. לוחצים **Create Web Service** ומחכים 2–3 דקות.
   כשמופיע **Live** בירוק – האתר באוויר!
7. למעלה מופיעה הכתובת. לוחצים עליה, וזהו – זה האתר שלך. 🎉

---

## שלב 3 · הופכים את עצמך למנהל

1. נכנסים לכתובת החדשה.
2. במסך הכניסה בוחרים **"אני חדש כאן"** ואז לוחצים על **"אני מנהל הקייטנות"**.
3. ממלאים שם פרטי, שם משפחה, סיסמה, ובשדה קוד מנהל את מה שכתבתם ב-`ADMIN_CODE`
   (או `1108` אם לא הגדרתם).
4. עכשיו מופיע לך כפתור **"ניהול קייטנות"** – שם מוסיפים ומוחקים קייטנות.

---

## שלב 4 · מפיצים לכולם

שולחים את הקישור בקבוצת הווטסאפ של הכיתה. כל אחד נכנס, כותב שם וסיסמה,
ומתחיל לדרג. אפשר להיכנס מהטלפון, מהמחשב, מכל מקום.

---

## דברים שכדאי לדעת

- **הכניסה הראשונה בכל בוקר תהיה איטית** (בערך דקה). זה כי בתוכנית החינמית של
  Render השרת "נרדם" אחרי 15 דקות בלי מבקרים ומתעורר כשמישהו נכנס. אחרי
  שהתעורר – הכול מהיר.
- **הדירוגים מתאפסים מדי פעם.** בלי מסד נתונים, הנתונים נשמרים בקובץ על השרת –
  ו-Render מוחק אותו בכל פעם שהשרת נרדם או עולה מחדש. מצוין להראות לחברים,
  פחות טוב לטווח ארוך. הפתרון בהמשך.
- **רוצה לשנות משהו באתר?** מעלים את הקובץ המעודכן ל-GitHub, ו-Render מעדכן
  את האתר לבד תוך כמה דקות.
## אם רוצים שהדירוגים יישמרו לתמיד

מוסיפים מסד נתונים, וזה הכול – אין מה לשנות בקוד:

**האפשרות המהירה (בתוך Render):** New + → **Postgres** → Free → Create.
נכנסים למסד שנוצר, מעתיקים את ה-**Internal Database URL**, חוזרים ל-Web Service →
**Environment** → Add Environment Variable בשם `DATABASE_URL` ומדביקים.
⚠️ מסד נתונים חינמי ב-Render פג אחרי 30 יום. אם זה קורה – האתר ממשיך לעבוד,
פשוט חוזר לשמור בקובץ, ואפשר ליצור מסד חדש.

**האפשרות לטווח ארוך:** חשבון חינמי ב-https://neon.com, יוצרים פרויקט,
מעתיקים את ה-**Connection string** (מתחיל ב-`postgresql://`) ומדביקים אותו
באותו משתנה `DATABASE_URL`. לא פג אחרי 30 יום.

## אם משהו לא עובד

| מה רואים | מה עושים |
|---|---|
| ב-Render כתוב **Failed** | לוחצים על **Logs** ומסתכלים בשורה האדומה האחרונה |
| בלוג כתוב `Could not connect to the database` | ה-`DATABASE_URL` לא נכון או פג – האתר ימשיך לעבוד בלי שמירה קבועה |
| האתר נטען אבל הכניסה נכשלת | מחכים דקה (השרת מתעורר) ומנסים שוב |
| האתר נפתח ריק | בודקים שהעליתם ל-GitHub גם את התיקייה `public` |
| מופיעות 0 קייטנות | לא הועלה הקובץ `camps.json` – מעלים אותו ומחכים לפריסה מחדש |
